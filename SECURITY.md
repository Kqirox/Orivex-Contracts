# Security

For critical / fund-loss bugs, please email `security@orivex.example` rather
than filing a public issue. Non-critical issues can be reported via a normal
GitHub issue with the `security` label.
