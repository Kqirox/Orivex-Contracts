No explanation of any part of the code. Only the code. Only the explanation. Only the code. Only the explanation. Only the code. Only the explanation. Only the code. Only the explanation. Only the code. Only the explanation. Only the code. Only the explanation. Only the code. Only the explanation. Only the code. Only the explanation. Only the code. Only the explanation. Only the code. Only the explanation. Only the code. Only the explanation. Only the code. Only the explanation. Only the code. Only the explanation. Only the code. Only the explanation. Only the code. Only the explanation. Only the code. Only the explanation. Only the code. Only the explanation. Only the code. Only the explanation. Only the code. Only the explanation. Only the code. Only the explanation. Only the code. Only the explanation. Only the code. Only the explanation. Only the code. Only the explanation. Only the code. Only the explanation. Only the code. Only the explanation. Only the code. Only the explanation. Only the code. Only the explanation. Only the code. Only the explanation. Only the code. Only the explanation. Only the code. Only the explanation. Only the code. Only the explanation. Only the code. Only the explanation. Only the code. Only the explanation. Only the code. Only the explanation. Only the code. Only the explanation. Only the code. Only the explanation. Only the